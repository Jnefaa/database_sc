{% extends 'base.html.twig' %}
{% block title %}grade{% endblock %}
{% block body %}
 <h1>{{ title }}</h1>
<div style="border-style: dashed;border-color: slategrey; margintop: auto; width: auto">
 <table class="table table-hover" style="margin-top: 15px">
 <thead class="bg-primary" style="background-color:white">
 <tr class="table-primary">
 <th>Id</th>
 <th>Libelle</th>
 <th>Description</th>
 <th>actions</th>
 </tr>
 </thead>
 <tbody>
 {% for grade in grades %}
 <tr>
 <td>{{ grade.id }}</td>
 <td>{{ grade.Libelle }}</td>
 <td>{{ grade.Description }}</td>
 <td>
 <a class="badge rounded-pill bg-primary" href="{{
path('grade_show', {'id': grade.id}) }}">Apperçu</a>
 <a class="badge rounded-pill bg-success" href="{{
path('grade_edit', {'id': grade.id}) }}">Editer</a>
 <a class="badge rounded-pill bg-danger" href="{{
path('grade_delete', {'id': grade.id}) }}" onclick="return
confirm('Are you sure you want to delete this item?');">Supprimer</a>
 <i class="bi bi-apple"></i>
 </td>
 </tr>
 {% else %}
 <tr>
 <td colspan="4">no records found</td>
 </tr>
 {% endfor %}
 </tbody>
 </table>
</div>
 <a style="margin-top: 10px" class="btn btn-primary" href="{{
path('addgradeLink') }}">Ajouter grade</a>
{% endblock %}